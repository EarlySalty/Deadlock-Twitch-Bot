# Trivy-Ausnahmen. Jeder Eintrag braucht eine Begruendung im statement.
secrets:
  - id: discord-client-id
    paths:
      - "rust/crates/tb-dashboard-api/src/auth/discord_admin_login.rs"
    statement: >-
      Testfixture im cfg(test)-Modul: discord_id 123456789012345678 ist eine
      Platzhalterzahl fuer die Session-Tests, keine echte Discord-Client-ID.
