# Werbemanager Matchschutz (Paket T)

Status: in Arbeit. Intent: diese ChatGPT-Session, kein T3-Elternthread.

Nutzerauftrag: Werbung während laufendem Deadlock-Match verhindern, Queue als Werbefenster nutzen und geplante Twitch-Werbung passend vorziehen/verschieben. Du bist der einzige Thread für dieses Paket. Keine Unter-Threads oder Unter-Agenten spawnen. Keine Nachrichten an andere Sessions. Schreibbereich ausschließlich /home/nathanael/.worktrees/tb-admanager-steam-api-20260924 auf Branch fix/admanager-steam-api-20260924, EarlySalty/Deadlock-Twitch-Bot. Der Worktree ist unser sauberer eigener Stand. Fremde Worktrees und CI-Branches unverändert lassen.

Vor jeder Arbeit geltende Regeln lesen. PR-first vom 24.09.2026 hat Vorrang, Datei /home/nathanael/.worktrees/claude-config-pr-first-testbetrieb-20260924/orchestrierung/PR-FIRST-TESTBETRIEB.md. Kein Merge/main-Push/Auto-Merge/Deploy/Restart/Produktionsmigration/Cleanup. Keine echten Ads oder Chatnachrichten als Test auslösen. Workflow-/Ruleset-Arbeit gehört fremden CI-Sessions. Keine Secrets ausgeben, nur vorgesehene Loader und isolierte Testdatenbanken verwenden.

Bestätigter Ausgangsbefund: AdManagerStore::steam_match_summary in rust/crates/tb-analytics/src/ad_manager.rs JOINt twitch_engagement_settings mit activity.live_player_state in demselben self.pool. Die reale Laufzeit ist getrennt: twitch_analytics enthält keine activity.live_player_state, Steam schreibt zentrale DB. Fehler werden als None verschluckt; Worker und UI tun so, als sei Steam nicht verbunden. Bei earlysalty: enabled=true, smart, action_lead_seconds=60, snooze_count=3, gesunder Heartbeat, match_active=false, twitch_plan_active. Letzte Aktion war Snooze am 21.09.; keine Commercial-Aktion in der gelesenen Historie. Zusätzlicher Fehler: für earlysalty gab es bei Diagnose keine twitch_engagement_settings-Zeile. Die modernere freiwillige Steam-Verknüpfung läuft über twitch_player_steam_links/Steam-Konten und stabile Twitch-ID. Der bereits gemergte Match-Hardlock (f6579231,7b6a8807) ist vorhanden und reicht ohne funktionierende Statusquelle nicht.

Baue einen gemeinsamen, testbaren Steam-Lesepfad für Worker UND Dashboard, ohne zentralen DSN, FDW oder Steam-Tabellen im Twitch-Pool. Zuerst aktuellen Bestand ansehen (stats.rs, player_links.rs, steam_lookup.rs, bestehende interne HTTP-Clients). Folgender Producer-Vertrag wird in unserem getrennten Steam-Paket S gebaut, kein Warten oder Session-Koordination nötig:
GET http://127.0.0.1:8783/internal/player-live?steam_id=<SteamID64>, X-Internal-Token mit bestehendem Secret. JSON found bool, in_match bool, in_deadlock bool, hero string|null, minutes integer|null, stage string|null, last_update Unix-Sekunden|null. last_update echter Quellzeitpunkt. Nicht gefunden found=false; DB-Fehler503, ungültige ID400; fehlender Server-Token blockt. Für Discord-Zuordnung existiert GET /player-live?discord_id=... mit linked, live, in_deadlock, hero, minutes, stage, last_update (Unix-Sekunden). Keine öffentliche neue Route nötig.

Identität: stabile twitch_user_id aus ManagedChannel/Auth benutzen. Vorhandene freiwillige Twitch/Steam-Zuordnung bevorzugen und lookup_enabled=false respektieren; ausgewähltes primäres Konto nicht durch anderes Konto ersetzen. Falls kein direkter Link, vorhandene explizit gepflegte Engagement-Steam-ID (über die aktuelle per Twitch-ID belegte Kanalzuordnung) oder gespeicherte Discord-Verknüpfung mit bestehendem Steam-Bot-Vertrag verwenden. Kein Namensraten. Abhängigkeiten sparsam, keine Zyklen tb-chat/tb-analytics. Kanalverknüpfung nach externem Lookup revalidieren, damit Trennen/Ändern nicht mit altem Status übersprungen wird.

Ausfälle konservativ: HTTP-Fehler/fehlende/veraltete/inkonsistente Presence ist UNBEKANNT, keine sichere Queue. Keine eigenen automatischen Commercials/Vorziehen aus ruhigem Chat oder least_bad bei unbekanntem Matchstatus. Für eine imminent geplante Twitch-Werbung bei unbekanntem Status oder erkanntem Match vorhandene Snoozes nutzen, ohne eigenen Ersatzblock. Queue/Menü mit frischem eindeutigem Nicht-Match weiterhin zulassen; keine Werbung ab erstem erkannten Match-Tick. Frische max180s plus Zukunftsprüfung; fehlende booleans nicht false-defaulten. Snooze muss innerhalb des 25s-Worker-Taktes noch rechtzeitig planbar sein (kurze konfigurierbare Lead-Werte prüfen). Bestehende Start-/Raid-/Cooldown-/Budget-/Idempotenzregeln und manuelle Aktionen nicht beiläufig verändern.

Observability: UI darf einen Quellenfehler nicht als nicht verbunden oder gesund geschützten Matchstatus verkaufen. Klare unterscheidbare Zustände/Reason Codes für unknown/unavailable/stale, sensible Details nicht ausgeben. Wiederkehrende Fehler nicht je25s spammen. Heartbeat darf keine erfolgreiche Matchprüfung vortäuschen. Ungesicherte Werbesprüche wie 'dafür bleibt das Match später frei' entfernen, Twitch-Pausen sind endlich. Strategie-Karten optisch beibehalten, nur notwendige Status-/Fehlertexte und bestehende WERBEMANAGER-Doku korrigieren (alte erste Matchminute darf kein Werbefenster mehr sein). Kein CHANGELOG.md.

Tests mit mock HTTP plus isoliertem Twitch-Schema OHNE activity-Schema. Belegen: moderne/Legacy-Verknüpfung, Opt-out, falsches Konto/Änderung, frisches Match sofort geschützt, Queue erlaubt, unbekannt/stale/500/fehlende Felder nicht als Freigabe, Snooze bei imminenter Werbung und vorhandenem Guthaben, null Guthaben kein eigener Ersatzblock. Bestehende tb-analytics ad_manager_decision/store, tb-bot ad_manager_wiring und dashboard API/frontend-Tests nachziehen. Toolchain nicht /usr/bin/cargo (veraltet), rustup/current Cargo explizit nutzen; -j2, keine produktiven Builds.

Verifizierte Commits sofort push -u origin fix/admanager-steam-api-20260924; vor Commit log-1/status/worktree list. PR eröffnen (Abhängigkeit zu Steam-Paket S als notwendige Auslieferungsvoraussetzung nennen), aktuelle Actions auswerten, fixe Codeursachen beheben. EVIDENCE.md: voller SHA, Commands, echte Testanzahl/Skips, Runs/PR, Abhängigkeit/Blocker. Kein Erfolg vortäuschen. Keine eigene externe Review erfinden. Endbericht kurz mit PR, SHA, Tests, Änderungen und offenen Punkten.
