# Nicht geheime Laufzeitkonfiguration. Der gemeinsame Teil wird beim Deploy aus
# der bestehenden normalen Bot-Konfiguration installiert.
source /etc/deadlock-twitch/runtime-common.conf

# Peer-Authentifizierung: Unix-Konto twitchdash == PostgreSQL-Rolle twitchdash.
TWITCH_DASHBOARD_ANALYTICS_DSN='postgresql:///twitch_analytics?host=/var/run/postgresql&application_name=tb-dashboard'
