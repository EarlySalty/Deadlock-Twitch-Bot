# Hash-locked dependencies for pytest CI.
# Keep this list aligned with pyproject.toml direct runtime and test dependencies.
aiohttp>=3.13.5
anthropic>=0.40
coverage[toml]>=7.0
discord.py>=2.3
fpdf2>=2.8
keyring>=24
psycopg[binary]>=3.1
pytest>=8.0
pytest-cov>=7.0
stripe>=11
twitchio>=2.10
