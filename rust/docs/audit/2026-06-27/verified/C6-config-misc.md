# C6 Verifikation: Config / Misc

Scope: adversariale Gegenpruefung der genannten Befunde gegen `00-baseline.md` Abschnitt 2. Keine Git-/Secret-Nutzung; statische Source-Inspection per `rg`, `sed`, `nl`.

| Befund | Verdict | Begründung / Beleg |
|---|---|---|
| B1-015 / B7-004 `!engagement_on` / `!engagement_off` | COSMETIC/LOW | Chat-Komfort fehlt real, aber Aktivierung ist erreichbar. Rust-Chat verdrahtet nur `!engagement_status`, `!engagement_ignore_me`, `!engagement_remember_me` (`rust/crates/tb-chat/src/commands.rs:452`, `rust/crates/tb-chat/src/commands.rs:456`, `rust/crates/tb-chat/src/commands.rs:460`); Python hatte `engagement_on/off` (`bot/chat/engagement_commands.py:101`, `bot/chat/engagement_commands.py:127`). Dashboard-Routen existieren: `POST /twitch/api/v2/engagement/toggle` setzt `enabled` (`rust/crates/tb-dashboard-api/src/lib.rs:270`, `rust/crates/tb-dashboard-api/src/handlers/engagement_settings.rs:185`) und `POST /twitch/api/v2/engagement/mode` setzt `off|shadow|live` (`rust/crates/tb-dashboard-api/src/lib.rs:287`, `rust/crates/tb-dashboard-api/src/handlers/engagement_mode.rs:112`). Die Pipeline verlangt `enabled=true` plus `output_mode!=off` (`rust/crates/tb-engagement/src/pipeline.rs:181`, `rust/crates/tb-engagement/src/pipeline.rs:189`). Also nicht P1 "nicht aktivierbar", sondern fehlende Chat-Paritaet. |
| B9-020 `connect_timeout` geladen, aber nicht angewandt | REAL-GAP | Nicht im Intent-Ledger als Drop gedeckt. `DbConfig` liest `TWITCH_ANALYTICS_CONNECT_TIMEOUT_SECONDS` in `connect_timeout` (`rust/crates/tb-config/src/lib.rs:42`, `rust/crates/tb-config/src/lib.rs:61`), `tb_db::connect` nutzt aber nur `pool_max`, `acquire_timeout` und `dsn` (`rust/crates/tb-db/src/pool.rs:8`, `rust/crates/tb-db/src/pool.rs:11`). Python injiziert den Timeout in den DSN (`bot/storage/pg.py:142`, `bot/storage/pg.py:689`). Operativer Failure-Behavior-Gap, P2 plausibel. |
| B9-021 `TWITCH_ALERT_CHANNEL_ID` / `TWITCH_STATS_CHANNEL_IDS` | REAL-GAP, teilweise entschärft | Alert-Teil real: Python hatte `TWITCH_ALERT_CHANNEL_ID` als Config-Konstante (`bot/core/constants.py:30`); Rust hardcoded dieselbe ID als Moderations-Alert (`rust/crates/tb-chat/src/pipeline.rs:145`) und Self-Explainer-Log (`rust/crates/tb-dashboard-api/src/handlers/self_explainer.rs:47`). Keine Ledger-Entscheidung belegt den Alert-Channel-Drop; nur Discord-Gateway/Broker-Ersatz ist dokumentiert (`rust/docs/audit/2026-06-27/00-baseline.md:70`). Stats-Teil ist dagegen kein starker Bug: `TWITCH_STATS_CHANNEL_IDS` gateete den Discord-`!twl`-Command (`bot/core/constants.py:33`, `bot/community/leaderboard.py:404`), und der interaktive Discord-Leaderboard wurde bewusst "DROP aus Discord", Web-Leaderboard neu im Dashboard (`rust/docs/audit/2026-06-15-grillme-entscheidungen.md:306`, `rust/crates/tb-dashboard-api/src/handlers/leaderboard.rs:1`). |
| B5a-004 `/healthz`, `/readyz`, `/health` nativ fehlend | REAL-GAP | Python-Dashboard-Service registriert alle drei Root-Probes (`bot/dashboard_service/app.py:773`, `bot/dashboard_service/app.py:774`, `bot/dashboard_service/app.py:775`). Im Rust-Dashboard ist nur Admin-System-Health unter `/twitch/api/admin/system/health` geroutet (`rust/crates/tb-dashboard-api/src/lib.rs:562`, `rust/crates/tb-dashboard-api/src/lib.rs:568`). Internal-API hat `/internal/twitch/v1/healthz` (`rust/crates/tb-internal-api/src/lib.rs:75`), ersetzt aber keine Dashboard-root Liveness/Readiness-Probes. Baseline belegt native Dashboard/API-Flaeche (`rust/docs/audit/2026-06-27/00-baseline.md:57`) und Admin-Health als behoben (`rust/docs/audit/2026-06-27/00-baseline.md:227`), keinen bewussten Drop der Root-Probes. |
| B8-011 Transcription Engines fehlen | DELIBERATE | Ja, der Drop ist im Ledger belegt. Grillme Block 15 sagt explizit: "Transkription raus" (`rust/docs/audit/2026-06-15-grillme-entscheidungen.md:322`) und `transcription-1 faster_whisper` / `transcription-3 seed_vocab` -> `DROP/OFF` (`rust/docs/audit/2026-06-15-grillme-entscheidungen.md:329`). Querschnittsdirektive streicht OpenAI/Whisper (`rust/docs/audit/2026-06-15-grillme-entscheidungen.md:366`); Baseline fuehrt Auto-Upload/Clip-Creation/Transcription als nicht default-aktiv/drop-gated (`rust/docs/audit/2026-06-27/00-baseline.md:54`). Rust-Code passt dazu: nur `Transcriber`-Trait, kein injizierter Transcriber (`rust/crates/tb-social-media/src/enrich_pipeline.rs:44`, `rust/bin/tb-bot/src/main.rs:1201`). Kein Bug gegen aktuellen Intent. |
| B9-019 / B7-008 `twitch_engagement_sender_auth` nicht in SQLx-Migrationen | REAL-GAP | Runtime-Funktion existiert, aber widerspricht dem Schema-Intent. Rust erstellt die Tabelle zur Laufzeit (`rust/crates/tb-engagement/src/sender_auth.rs:113`, `rust/crates/tb-engagement/src/sender_auth.rs:117`), analog Python (`bot/engagement/sender_auth.py:77`, `bot/engagement/sender_auth.py:82`). Der Rust-Intent ist aber DDL-SSOT in `rust/migrations/`: ADR 0002 nennt sqlx-native als einzige DDL-SSOT (`rust/docs/adr/0002-db-sqlx-refinery-shared-schema.md:20`), Cleanup sagt verstreute DDLs ziehen in ein Migrationsverzeichnis (`rust/docs/05-cleanup-decisions.md:7`, `rust/docs/05-cleanup-decisions.md:10`), Baseline nennt SQLx-Migrationen statt `ensure_schema`/Godclass (`rust/docs/audit/2026-06-27/00-baseline.md:59`). `rg "twitch_engagement_sender_auth" rust/migrations` ergab keinen Treffer. |
| B9-024 / B7-009 / B4-025 `tb-llm` Provider-Abstraktion nicht adoptiert | COSMETIC/LOW | Kein Funktions-Bug, sondern unvollstaendige Zentralisierung. `tb-llm` beschreibt sich als "Phase-0-Foundation" und sagt, bestehende verstreute Calls koennen gegen `provider::LlmProvider` programmieren (`rust/crates/tb-llm/src/lib.rs:1`, `rust/crates/tb-llm/src/lib.rs:20`); Provider-Auswahl liegt bewusst beim Aufrufer (`rust/crates/tb-llm/src/provider.rs:8`). Produktion nutzt weiter direkte Clients: Engagement-eigener MiniMax-Client (`rust/crates/tb-engagement/src/minimax_chat.rs:242`), Post-Stream direkte MiniMax/Claude-HTTP-Calls (`rust/crates/tb-analytics/src/post_stream.rs:412`, `rust/crates/tb-analytics/src/post_stream.rs:469`) und Social-Media-eigene Provider (`rust/crates/tb-social-media/src/llm_dispatch.rs:140`, `rust/crates/tb-social-media/src/llm_dispatch.rs:219`). Funktionsverlust ist nicht belegt: direkte Pfade nutzen MiniMax/Anthropic und verbuchen Ledger zumindest in zentralen MiniMax-Pfaden (`rust/crates/tb-engagement/src/minimax_chat.rs:316`, `rust/crates/tb-analytics/src/post_stream.rs:436`). Severity runter: Architektur-Schuld, keine P2-Funktionsregression. |

Kurzfazit: Real bleiben B9-020, der Alert-Teil von B9-021, B5a-004 und B9-019. B8-011 ist deliberate. B1-015 und B9-024 sind herabzustufen, weil Aktivierung bzw. LLM-Funktionalitaet weiterhin erreichbar ist.
