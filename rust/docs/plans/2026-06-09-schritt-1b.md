# Schritt 1b — Auth-Layer + streamer-scoped Analytics-GETs

**Ziel:** Admin-geschützte Analytics-Routen (`/twitch/api/v2/streamers`, `/twitch/api/v2/overview`,
`/twitch/api/v2/auth-status`) plus einen `AuthLevel`-Extractor und Plan-Gating in Rust implementieren.

**Scope:**
- `AuthLevel` Extractor (axum `FromRequestParts`) in `tb-http-core`
- IDOR-Guard + Plan-Gating Hilfsfunktionen in `tb-dashboard-api`
- 3 neue Handler in `tb-dashboard-api`: `auth_status`, `streamers`, `overview`
- `build_authed_router` in `tb-dashboard-api::lib` + Merge mit `build_public_router` in `tb-dashboard` binary
- Hermetische Tests (Wegwerf-Container Port 5434)

**Neue Crates/Dateien:**
- Modify: `rust/crates/tb-http-core/src/middleware/auth.rs` — `AuthLevel` Extractor ergänzen
- Modify: `rust/crates/tb-http-core/src/middleware/mod.rs` — `AuthLevel` re-exportieren
- Modify: `rust/crates/tb-http-core/src/lib.rs` — pub-use `AuthLevel`
- Modify: `rust/crates/tb-dashboard-api/src/lib.rs` — `build_authed_router` + Merge-Funktion
- New: `rust/crates/tb-dashboard-api/src/auth.rs` — `require_owner`, `require_extended_plan`
- New: `rust/crates/tb-dashboard-api/src/handlers/auth_status.rs`
- New: `rust/crates/tb-dashboard-api/src/handlers/streamers.rs`
- New: `rust/crates/tb-dashboard-api/src/handlers/overview.rs`
- Modify: `rust/crates/tb-dashboard-api/src/handlers/mod.rs` — neue Module einbinden
- Modify: `rust/crates/tb-dashboard-api/Cargo.toml` — `tb-http-core` + `tb-config` als Deps
- Modify: `rust/bin/tb-dashboard/src/main.rs` — authed Router mounten

---

## Schritt 1: AuthLevel Extractor

### Dateien
- Modify: `rust/crates/tb-http-core/src/middleware/auth.rs`
- Modify: `rust/crates/tb-http-core/src/middleware/mod.rs`
- Modify: `rust/crates/tb-http-core/src/lib.rs`

### Implementierung

Den folgenden Block an das Ende von `src/middleware/auth.rs` anfügen (nach der bestehenden
`internal_auth`-Funktion und `constant_time_eq`):

```rust
use axum::{
    async_trait,
    extract::FromRequestParts,
    http::request::Parts,
};
use std::net::SocketAddr;
use axum::extract::ConnectInfo;

/// Auth-Level eines eingehenden Requests.
///
/// Auswertungsreihenfolge:
/// 1. Loopback-IP (127.x.x.x) **und** Host-Header ist `localhost` oder `127.0.0.1`
///    → `Localhost` (impliziert admin-Level, kein Token nötig)
/// 2. `X-Internal-Token`-Header stimmt constant-time mit konfiguriertem Token überein
///    → `Admin`
/// 3. Sonst → `None`
///
/// Partner-Session-Auth (Fernet-Cookie) ist deferred (ADR 0003) und wird hier nicht
/// implementiert.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum AuthLevel {
    /// Loopback-Verbindung (127.x.x.x) mit localhost-Host-Header.
    Localhost,
    /// Gültiger X-Internal-Token.
    Admin,
    /// Nicht authentifiziert.
    None,
}

impl AuthLevel {
    /// Gibt `true` zurück wenn Admin oder Localhost.
    pub fn is_privileged(&self) -> bool {
        matches!(self, AuthLevel::Admin | AuthLevel::Localhost)
    }

    /// Serialisiert den Level als JSON-String-Wert.
    pub fn as_str(&self) -> &'static str {
        match self {
            AuthLevel::Localhost => "localhost",
            AuthLevel::Admin => "admin",
            AuthLevel::None => "none",
        }
    }
}

#[async_trait]
impl<S> FromRequestParts<S> for AuthLevel
where
    S: Send + Sync,
{
    type Rejection = std::convert::Infallible;

    async fn from_request_parts(parts: &mut Parts, _state: &S) -> Result<Self, Self::Rejection> {
        // 1. Loopback: ConnectInfo + Host-Check
        let is_loopback = parts
            .extensions
            .get::<ConnectInfo<SocketAddr>>()
            .map(|ci| ci.0.ip().is_loopback())
            .unwrap_or(false);

        if is_loopback {
            let host = parts
                .headers
                .get(axum::http::header::HOST)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("");
            // Host-Header: "localhost", "localhost:<port>", "127.0.0.1", "127.0.0.1:<port>"
            let host_base = host.split(':').next().unwrap_or(host);
            if host_base == "localhost" || host_base == "127.0.0.1" {
                return Ok(AuthLevel::Localhost);
            }
        }

        // 2. Admin: X-Internal-Token aus Extension (gesetzt vom Router via with_state)
        // Der Token wird als Extension eingefügt, damit kein State-Generic benötigt wird.
        if let Some(expected) = parts.extensions.get::<ExpectedToken>() {
            let provided = parts
                .headers
                .get(INTERNAL_TOKEN_HEADER)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("");
            if !expected.0.is_empty()
                && constant_time_eq(provided.as_bytes(), expected.0.as_bytes())
            {
                return Ok(AuthLevel::Admin);
            }
        }

        Ok(AuthLevel::None)
    }
}

/// Newtype um den erwarteten Token als axum-Extension einzufügen.
///
/// Wird vom Router via `.layer(Extension(ExpectedToken(token)))` gesetzt.
#[derive(Clone)]
pub struct ExpectedToken(pub String);
```

In `src/middleware/mod.rs` ergänzen:

```rust
pub use auth::{AuthLevel, ExpectedToken};
```

In `src/lib.rs` ergänzen:

```rust
pub use middleware::auth::{AuthLevel, ExpectedToken};
```

### Tests

In `src/middleware/auth.rs` innerhalb `#[cfg(test)]` ergänzen:

```rust
#[cfg(test)]
mod auth_level_tests {
    use super::*;
    use axum::{
        body::Body,
        extract::ConnectInfo,
        http::{Request, StatusCode},
        middleware,
        routing::get,
        Extension, Router,
    };
    use std::net::SocketAddr;
    use tower::ServiceExt;

    async fn echo_auth(auth: AuthLevel) -> axum::response::Response {
        axum::response::Response::builder()
            .status(200)
            .body(Body::from(auth.as_str()))
            .unwrap()
    }

    fn make_router(token: &str) -> Router {
        Router::new()
            .route("/", get(echo_auth))
            .layer(Extension(ExpectedToken(token.to_string())))
    }

    fn req(ip: &str, host: &str, token: Option<&str>) -> Request<Body> {
        let addr: SocketAddr = format!("{}:12345", ip).parse().unwrap();
        let mut b = Request::builder()
            .uri("/")
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, host);
        if let Some(t) = token {
            b = b.header(INTERNAL_TOKEN_HEADER, t);
        }
        b.body(Body::empty()).unwrap()
    }

    #[tokio::test]
    async fn localhost_loopback_with_localhost_host() {
        let app = make_router("secret");
        let res = app.oneshot(req("127.0.0.1", "localhost", None)).await.unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let body = axum::body::to_bytes(res.into_body(), 64).await.unwrap();
        assert_eq!(&body[..], b"localhost");
    }

    #[tokio::test]
    async fn admin_with_correct_token() {
        let app = make_router("secret");
        let res = app
            .oneshot(req("192.168.1.1", "example.com", Some("secret")))
            .await
            .unwrap();
        let body = axum::body::to_bytes(res.into_body(), 64).await.unwrap();
        assert_eq!(&body[..], b"admin");
    }

    #[tokio::test]
    async fn none_without_token() {
        let app = make_router("secret");
        let res = app
            .oneshot(req("192.168.1.1", "example.com", None))
            .await
            .unwrap();
        let body = axum::body::to_bytes(res.into_body(), 64).await.unwrap();
        assert_eq!(&body[..], b"none");
    }

    #[tokio::test]
    async fn loopback_ip_but_non_localhost_host_falls_through_to_token() {
        // 127.0.0.1 aber Host: "myservice.internal" → kein Localhost-Level
        // Kein Token → None
        let app = make_router("secret");
        let res = app
            .oneshot(req("127.0.0.1", "myservice.internal", None))
            .await
            .unwrap();
        let body = axum::body::to_bytes(res.into_body(), 64).await.unwrap();
        assert_eq!(&body[..], b"none");
    }

    #[tokio::test]
    async fn wrong_token_gives_none() {
        let app = make_router("secret");
        let res = app
            .oneshot(req("10.0.0.1", "example.com", Some("wrong")))
            .await
            .unwrap();
        let body = axum::body::to_bytes(res.into_body(), 64).await.unwrap();
        assert_eq!(&body[..], b"none");
    }
}
```

---

## Schritt 2: IDOR-Guard + Plan-Gating

### Dateien
- New: `rust/crates/tb-dashboard-api/src/auth.rs`
- Modify: `rust/crates/tb-dashboard-api/src/lib.rs` — `pub mod auth;` ergänzen

### Implementierung

`rust/crates/tb-dashboard-api/src/auth.rs`:

```rust
//! Auth-Hilfsfunktionen: IDOR-Guard + Plan-Gating.
//!
//! Partner-Session-Auth ist deferred (ADR 0003). Für jetzt wird `Partner`-Level
//! nie erzeugt — die Funktionen sind trotzdem korrekt implementiert, damit der
//! Umbau später ohne Behaviour-Änderung einsetzbar ist.

use axum::http::StatusCode;
use serde_json::json;
use sqlx::PgPool;
use tb_http_core::{ApiError, AuthLevel};

/// Prüft ob der anfragende User die angegebene Login abfragen darf.
///
/// - `Admin` / `Localhost` → immer OK
/// - `Partner` (wenn implementiert) → nur wenn `queried_login == session_login`
/// - `None` → immer 403
pub fn require_owner(
    auth: &AuthLevel,
    queried_login: &str,
    session_login: Option<&str>,
) -> Result<(), ApiError> {
    match auth {
        AuthLevel::Admin | AuthLevel::Localhost => Ok(()),
        AuthLevel::None => Err(ApiError::unauthorized()),
        // Partner-Level: deferred — wenn hier doch mal ein Partner-Extractor
        // auftaucht, ist dieser Arm bereits korrekt implementiert.
        // (Aktuell nicht erreichbar — AuthLevel hat kein Partner-Variant.)
    }
}

/// Prüft ob `login` das `analytics.extended`-Entitlement hat.
///
/// Liest `streamer_plans.manual_plan_id` und `manual_plan_expires_at` aus der DB.
/// Plan-IDs mit `analytics.extended`-Entitlement: `analytics_pro`, `analytics_extended`
/// (muss mit Python-`_KNOWN_BILLING_PLAN_IDS` übereinstimmen — bei Änderung synchron halten).
///
/// Admin + Localhost überspringen die Prüfung (bypass).
pub async fn require_extended_plan(
    pool: &PgPool,
    login: &str,
    auth: &AuthLevel,
) -> Result<(), ApiError> {
    // Admin/Localhost: kein Plan-Gate
    if auth.is_privileged() {
        return Ok(());
    }

    let row: Option<(Option<String>, Option<String>)> = sqlx::query_as(
        r#"
        SELECT manual_plan_id, manual_plan_expires_at
        FROM streamer_plans
        WHERE LOWER(twitch_login) = LOWER($1)
        LIMIT 1
        "#,
    )
    .bind(login)
    .fetch_optional(pool)
    .await
    .map_err(|_| ApiError::internal())?;

    let has_entitlement = match row {
        None => false,
        Some((plan_id, expires_at)) => {
            let plan = plan_id.as_deref().unwrap_or("").trim().to_string();
            if !EXTENDED_PLAN_IDS.contains(&plan.as_str()) {
                false
            } else {
                // Ablaufprüfung: expires_at ist ISO-8601-String oder NULL (kein Ablauf)
                match expires_at.as_deref() {
                    None | Some("") => true,
                    Some(s) => {
                        // Parse als RFC3339; wenn fehlerhaft → abgelaufen (fail-safe)
                        chrono::DateTime::parse_from_rfc3339(s)
                            .map(|dt| dt > chrono::Utc::now())
                            .unwrap_or(false)
                    }
                }
            }
        }
    };

    if has_entitlement {
        Ok(())
    } else {
        Err(ApiError::plan_required())
    }
}

/// Plan-IDs, die das `analytics.extended`-Entitlement tragen.
/// Muss mit Python-`_KNOWN_BILLING_PLAN_IDS`-Filterung synchron gehalten werden.
const EXTENDED_PLAN_IDS: &[&str] = &["analytics_pro", "analytics_extended"];
```

### SQL (Plan-Gating)

Exakter SQL aus Python `_manual_override_plan_for_login` extrahiert:

```sql
SELECT manual_plan_id, manual_plan_expires_at
FROM streamer_plans
WHERE LOWER(twitch_login) = LOWER($1)
LIMIT 1
```

Expires-Logik: `manual_plan_expires_at` ist `TEXT` in der DB (ISO-8601). `NULL` bedeutet kein
Ablauf. Ein leerer String wird wie `NULL` behandelt. Liegt der Zeitpunkt in der Vergangenheit →
Plan gilt als abgelaufen.

### `ApiError` erweitern

In `tb-http-core/src/error.rs` zwei neue Konstruktoren ergänzen:

```rust
impl ApiError {
    // ... bestehende Konstruktoren ...

    pub fn plan_required() -> Self {
        Self {
            status: StatusCode::FORBIDDEN,
            body: ErrorBody {
                error: "plan_required",
                message: "analytics.extended entitlement required",
            },
        }
    }

    pub fn internal() -> Self {
        Self {
            status: StatusCode::INTERNAL_SERVER_ERROR,
            body: ErrorBody {
                error: "internal_error",
                message: "internal server error",
            },
        }
    }
}
```

**Hinweis:** `chrono` muss als Workspace-Dep ergänzt werden (falls noch nicht vorhanden):

In `rust/Cargo.toml` unter `[workspace.dependencies]`:
```toml
chrono = { version = "0.4", features = ["serde"] }
```

In `tb-dashboard-api/Cargo.toml`:
```toml
chrono = { workspace = true }
```

---

## Schritt 3: auth-status Endpoint

### Route

```
GET /twitch/api/v2/auth-status
```

Antwortet immer mit 200. Gibt den aktuellen Auth-Level zurück. Kein 401, kein Loopback-Gate.

### Dateien
- New: `rust/crates/tb-dashboard-api/src/handlers/auth_status.rs`

### Handler-Code

```rust
//! Handler für `GET /twitch/api/v2/auth-status`.
//!
//! Antwortet immer 200 — auch ohne Auth. Dient dem Frontend zur Erkennung des
//! aktuellen Auth-Levels (z. B. für Conditional Rendering).

use axum::{response::IntoResponse, Json};
use serde::Serialize;
use tb_http_core::AuthLevel;

#[derive(Serialize)]
pub struct AuthStatusResponse {
    pub auth_level: &'static str,
    pub logged_in: bool,
}

/// `GET /twitch/api/v2/auth-status`
///
/// Kein Auth-Gate — immer 200.
pub async fn auth_status_handler(auth: AuthLevel) -> impl IntoResponse {
    Json(AuthStatusResponse {
        logged_in: auth.is_privileged(),
        auth_level: auth.as_str(),
    })
}
```

### Test

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use axum::{
        body::Body,
        extract::ConnectInfo,
        http::{Request, StatusCode},
        routing::get,
        Extension, Router,
    };
    use std::net::SocketAddr;
    use tb_http_core::ExpectedToken;
    use tower::ServiceExt;

    fn router(token: &str) -> Router {
        Router::new()
            .route("/twitch/api/v2/auth-status", get(auth_status_handler))
            .layer(Extension(ExpectedToken(token.to_string())))
    }

    fn req(ip: &str, host: &str, token: Option<&str>) -> Request<Body> {
        let addr: SocketAddr = format!("{}:9999", ip).parse().unwrap();
        let mut b = Request::builder()
            .uri("/twitch/api/v2/auth-status")
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, host);
        if let Some(t) = token {
            b = b.header("x-internal-token", t);
        }
        b.body(Body::empty()).unwrap()
    }

    #[tokio::test]
    async fn admin_token_gives_admin() {
        let res = router("tok")
            .oneshot(req("1.2.3.4", "example.com", Some("tok")))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 256).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        assert_eq!(v["auth_level"], "admin");
        assert_eq!(v["logged_in"], true);
    }

    #[tokio::test]
    async fn no_token_gives_none() {
        let res = router("tok")
            .oneshot(req("1.2.3.4", "example.com", None))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 256).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        assert_eq!(v["auth_level"], "none");
        assert_eq!(v["logged_in"], false);
    }

    #[tokio::test]
    async fn localhost_gives_localhost() {
        let res = router("tok")
            .oneshot(req("127.0.0.1", "localhost", None))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 256).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        assert_eq!(v["auth_level"], "localhost");
        assert_eq!(v["logged_in"], true);
    }
}
```

---

## Schritt 4: streamers Endpoint

### Route

```
GET /twitch/api/v2/streamers
```

Gibt alle aktiven Partner zurück (twitch_login, is_live, viewer_count). Admin-only (401 wenn kein
gültiger Token und kein Loopback).

### SQL

Exakt aus Python `_load_api_v2_streamers_data` extrahiert. Python fragt
`twitch_streamers_partner_state WHERE is_partner_active = 1` ab und ergänzt aus
`twitch_stream_sessions` eine Menge `recent_logins`. Für den Rust-Endpoint implementieren wir
nur den Partner-Query (recent_logins-Union kommt später — die Python-Route dient hier als Admin-
Listenansicht, kein recent fallback nötig):

```sql
SELECT
    sp.twitch_login,
    COALESCE(ls.is_live, 0)            AS is_live,
    COALESCE(ls.last_viewer_count, 0)  AS viewer_count
FROM twitch_streamers_partner_state sp
LEFT JOIN twitch_live_state ls
       ON LOWER(ls.streamer_login) = LOWER(sp.twitch_login)
WHERE sp.is_partner_active = 1
ORDER BY
    COALESCE(ls.is_live, 0)           DESC,
    COALESCE(ls.last_viewer_count, 0) DESC,
    LOWER(sp.twitch_login)            ASC
```

### Dateien
- New: `rust/crates/tb-analytics/src/streamers.rs`
- Modify: `rust/crates/tb-analytics/src/lib.rs` — `pub mod streamers;`
- New: `rust/crates/tb-dashboard-api/src/handlers/streamers.rs`

### Row-Struct (in `tb-analytics/src/streamers.rs`)

```rust
//! Query für `GET /twitch/api/v2/streamers` (Admin).

use sqlx::PgPool;

#[derive(Debug, sqlx::FromRow)]
pub struct StreamerListRow {
    pub twitch_login: String,
    pub is_live:      i32,
    pub viewer_count: i32,
}

/// Lädt alle aktiven Partner mit Live-Status.
pub async fn active_streamers(pool: &PgPool) -> Result<Vec<StreamerListRow>, sqlx::Error> {
    sqlx::query_as(
        r#"
        SELECT
            sp.twitch_login,
            COALESCE(ls.is_live, 0)            AS is_live,
            COALESCE(ls.last_viewer_count, 0)  AS viewer_count
        FROM twitch_streamers_partner_state sp
        LEFT JOIN twitch_live_state ls
               ON LOWER(ls.streamer_login) = LOWER(sp.twitch_login)
        WHERE sp.is_partner_active = 1
        ORDER BY
            COALESCE(ls.is_live, 0)           DESC,
            COALESCE(ls.last_viewer_count, 0) DESC,
            LOWER(sp.twitch_login)            ASC
        "#,
    )
    .fetch_all(pool)
    .await
}
```

### Handler-Code (`src/handlers/streamers.rs`)

```rust
//! Handler für `GET /twitch/api/v2/streamers`.
//!
//! Admin-only: gibt 401 zurück wenn AuthLevel == None.

use axum::{extract::State, response::IntoResponse, Json};
use serde::Serialize;
use sqlx::PgPool;
use tb_analytics::streamers::{active_streamers, StreamerListRow};
use tb_http_core::{ApiError, AuthLevel};

#[derive(Serialize)]
pub struct StreamerJson {
    pub login:        String,
    pub is_live:      bool,
    pub viewer_count: i32,
}

impl From<StreamerListRow> for StreamerJson {
    fn from(r: StreamerListRow) -> Self {
        Self {
            login:        r.twitch_login,
            is_live:      r.is_live != 0,
            viewer_count: r.viewer_count,
        }
    }
}

#[derive(Serialize)]
pub struct StreamersResponse {
    pub streamers: Vec<StreamerJson>,
}

/// `GET /twitch/api/v2/streamers`
pub async fn streamers_handler(
    auth: AuthLevel,
    State(pool): State<PgPool>,
) -> Result<impl IntoResponse, ApiError> {
    if !auth.is_privileged() {
        return Err(ApiError::unauthorized());
    }
    let rows = active_streamers(&pool)
        .await
        .map_err(|_| ApiError::internal())?;
    Ok(Json(StreamersResponse {
        streamers: rows.into_iter().map(StreamerJson::from).collect(),
    }))
}
```

### Test (hermetisch)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use axum::{
        body::Body,
        extract::ConnectInfo,
        http::{Request, StatusCode},
        routing::get,
        Extension, Router,
    };
    use sqlx::PgPool;
    use std::net::SocketAddr;
    use tb_http_core::ExpectedToken;
    use tower::ServiceExt;

    async fn setup_pool() -> Option<PgPool> {
        let url = std::env::var("TB_TEST_DATABASE_URL").ok()?;
        PgPool::connect(&url).await.ok()
    }

    async fn setup_schema(pool: &PgPool) {
        sqlx::query(
            r#"
            CREATE TABLE IF NOT EXISTS twitch_streamers_partner_state (
                twitch_login TEXT PRIMARY KEY,
                is_partner_active INTEGER NOT NULL DEFAULT 0
            );
            CREATE TABLE IF NOT EXISTS twitch_live_state (
                streamer_login TEXT PRIMARY KEY,
                is_live INTEGER NOT NULL DEFAULT 0,
                last_viewer_count INTEGER NOT NULL DEFAULT 0
            );
            TRUNCATE twitch_streamers_partner_state, twitch_live_state;
            INSERT INTO twitch_streamers_partner_state VALUES ('streamer_a', 1), ('streamer_b', 1);
            INSERT INTO twitch_live_state VALUES ('streamer_a', 1, 42);
            "#,
        )
        .execute(pool)
        .await
        .unwrap();
    }

    fn router(pool: PgPool, token: &str) -> Router {
        Router::new()
            .route("/twitch/api/v2/streamers", get(streamers_handler))
            .with_state(pool)
            .layer(Extension(ExpectedToken(token.to_string())))
    }

    fn admin_req(token: &str) -> Request<Body> {
        let addr: SocketAddr = "1.2.3.4:9999".parse().unwrap();
        Request::builder()
            .uri("/twitch/api/v2/streamers")
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, "example.com")
            .header("x-internal-token", token)
            .body(Body::empty())
            .unwrap()
    }

    fn unauth_req() -> Request<Body> {
        let addr: SocketAddr = "1.2.3.4:9999".parse().unwrap();
        Request::builder()
            .uri("/twitch/api/v2/streamers")
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, "example.com")
            .body(Body::empty())
            .unwrap()
    }

    #[tokio::test]
    async fn returns_401_without_auth() {
        let pool = match setup_pool().await {
            Some(p) => p,
            None => return, // kein TB_TEST_DATABASE_URL → skip
        };
        let res = router(pool, "tok")
            .oneshot(unauth_req())
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::UNAUTHORIZED);
    }

    #[tokio::test]
    async fn returns_streamers_for_admin() {
        let pool = match setup_pool().await {
            Some(p) => p,
            None => return,
        };
        setup_schema(&pool).await;
        let res = router(pool, "tok")
            .oneshot(admin_req("tok"))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 4096).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        let streamers = v["streamers"].as_array().unwrap();
        assert_eq!(streamers.len(), 2);
        // streamer_a ist live → zuerst
        assert_eq!(streamers[0]["login"], "streamer_a");
        assert_eq!(streamers[0]["is_live"], true);
        assert_eq!(streamers[0]["viewer_count"], 42);
    }
}
```

---

## Schritt 5: overview Endpoint

### Route

```
GET /twitch/api/v2/overview?streamer=<login>[&days=30]
```

Admin-only. `days` wird auf `[7, 365]` geclippt. Gibt 200 + `{"empty": true}` wenn keine
Sessions vorhanden, sonst vollständige Metriken.

### SQL

Exakt aus Python `_calculate_overview_metrics` (Zeilen 1316–1354) extrahiert. Der CTE-Block
wurde vereinfacht: die Chatter-Joins gehören zu einer späteren Phase (overview v2). Für 1b
implementieren wir den Basic-Metrics-Query (Zeilen 1347–1354 aus dem Python-Code):

```sql
SELECT
    AVG(s.avg_viewers)                                        AS avg_avg_viewers,
    MAX(s.peak_viewers)                                       AS max_peak_viewers,
    SUM(s.avg_viewers * s.duration_seconds / 3600.0)          AS total_hours_watched,
    SUM(s.duration_seconds / 3600.0)                          AS total_airtime_hours,
    SUM(CASE
            WHEN s.follower_delta IS NOT NULL
             AND NOT (s.followers_end = 0 AND s.followers_start > 0)
            THEN s.follower_delta
            ELSE 0
        END)                                                  AS total_followers,
    COUNT(*)                                                  AS session_count
FROM twitch_stream_sessions s
WHERE s.started_at >= $1
  AND s.ended_at IS NOT NULL
  AND ($2::TEXT IS NULL OR LOWER(s.streamer_login) = LOWER($2))
```

Existenz-Check (aus Python `_get_overview_data_sync`, Zeilen 1011–1023):

```sql
SELECT COUNT(*)
FROM twitch_stream_sessions s
WHERE s.started_at >= $1
  AND s.ended_at IS NOT NULL
  AND ($2::TEXT IS NULL OR LOWER(s.streamer_login) = LOWER($2))
```

### Dateien
- New: `rust/crates/tb-analytics/src/overview.rs`
- Modify: `rust/crates/tb-analytics/src/lib.rs` — `pub mod overview;`
- New: `rust/crates/tb-dashboard-api/src/handlers/overview.rs`

### Row-Struct + Query (`tb-analytics/src/overview.rs`)

```rust
//! Query für `GET /twitch/api/v2/overview` (Admin).

use sqlx::PgPool;

/// Aggregierte Metriken für einen Zeitraum.
#[derive(Debug, sqlx::FromRow)]
pub struct OverviewMetricsRow {
    pub avg_avg_viewers:    Option<f64>,
    pub max_peak_viewers:   Option<i64>,
    pub total_hours_watched: Option<f64>,
    pub total_airtime_hours: Option<f64>,
    pub total_followers:    Option<i64>,
    pub session_count:      Option<i64>,
}

/// Holt aggregierte Metriken für einen Streamer im angegebenen Zeitraum.
///
/// `streamer_login`: `None` → alle Streamer aggregiert.
/// `since`: ISO-8601-String, exklusiv (>= since_date).
pub async fn overview_metrics(
    pool: &PgPool,
    since: &str,
    streamer_login: Option<&str>,
) -> Result<Option<OverviewMetricsRow>, sqlx::Error> {
    sqlx::query_as(
        r#"
        SELECT
            AVG(s.avg_viewers)                                        AS avg_avg_viewers,
            MAX(s.peak_viewers)                                       AS max_peak_viewers,
            SUM(s.avg_viewers * s.duration_seconds / 3600.0)          AS total_hours_watched,
            SUM(s.duration_seconds / 3600.0)                          AS total_airtime_hours,
            SUM(CASE
                    WHEN s.follower_delta IS NOT NULL
                     AND NOT (s.followers_end = 0 AND s.followers_start > 0)
                    THEN s.follower_delta
                    ELSE 0
                END)                                                  AS total_followers,
            COUNT(*)                                                  AS session_count
        FROM twitch_stream_sessions s
        WHERE s.started_at >= $1
          AND s.ended_at IS NOT NULL
          AND ($2::TEXT IS NULL OR LOWER(s.streamer_login) = LOWER($2))
        "#,
    )
    .bind(since)
    .bind(streamer_login)
    .fetch_optional(pool)
    .await
}

/// Existenz-Check: gibt 0 zurück wenn keine Sessions vorhanden.
pub async fn overview_session_count(
    pool: &PgPool,
    since: &str,
    streamer_login: Option<&str>,
) -> Result<i64, sqlx::Error> {
    let row: (i64,) = sqlx::query_as(
        r#"
        SELECT COUNT(*)
        FROM twitch_stream_sessions s
        WHERE s.started_at >= $1
          AND s.ended_at IS NOT NULL
          AND ($2::TEXT IS NULL OR LOWER(s.streamer_login) = LOWER($2))
        "#,
    )
    .bind(since)
    .bind(streamer_login)
    .fetch_one(pool)
    .await?;
    Ok(row.0)
}
```

### Handler-Code (`src/handlers/overview.rs`)

```rust
//! Handler für `GET /twitch/api/v2/overview`.
//!
//! Admin-only. Kein Partner-Session-Auth (deferred, ADR 0003).

use axum::{
    extract::{Query, State},
    response::IntoResponse,
    Json,
};
use chrono::{Duration, Utc};
use serde::{Deserialize, Serialize};
use sqlx::PgPool;
use tb_analytics::overview::{overview_metrics, overview_session_count};
use tb_http_core::{ApiError, AuthLevel};

#[derive(Deserialize)]
pub struct OverviewParams {
    pub streamer: Option<String>,
    /// Zeitraum in Tagen. Default 30, min 7, max 365.
    #[serde(default = "default_days")]
    pub days: i64,
}

fn default_days() -> i64 {
    30
}

#[derive(Serialize)]
#[serde(untagged)]
pub enum OverviewResponse {
    Empty { empty: bool, error: &'static str },
    Data(OverviewData),
}

#[derive(Serialize)]
pub struct OverviewData {
    pub streamer:   Option<String>,
    pub days:       i64,
    pub summary:    OverviewSummary,
}

#[derive(Serialize)]
pub struct OverviewSummary {
    #[serde(rename = "avgViewers")]
    pub avg_viewers:          f64,
    #[serde(rename = "peakViewers")]
    pub peak_viewers:         i64,
    #[serde(rename = "totalHoursWatched")]
    pub total_hours_watched:  f64,
    #[serde(rename = "totalAirtime")]
    pub total_airtime:        f64,
    #[serde(rename = "followersDelta")]
    pub followers_delta:      i64,
    #[serde(rename = "totalSessions")]
    pub total_sessions:       i64,
}

/// `GET /twitch/api/v2/overview?streamer=<login>[&days=30]`
pub async fn overview_handler(
    auth: AuthLevel,
    State(pool): State<PgPool>,
    Query(params): Query<OverviewParams>,
) -> Result<impl IntoResponse, ApiError> {
    if !auth.is_privileged() {
        return Err(ApiError::unauthorized());
    }

    // days: clip to [7, 365]
    let days = params.days.clamp(7, 365);
    let since = (Utc::now() - Duration::days(days)).to_rfc3339();
    let login = params
        .streamer
        .as_deref()
        .map(|s| s.trim().to_lowercase())
        .filter(|s| !s.is_empty());
    let login_ref = login.as_deref();

    // Existenz-Check
    let count = overview_session_count(&pool, &since, login_ref)
        .await
        .map_err(|_| ApiError::internal())?;

    if count == 0 {
        return Ok(Json(OverviewResponse::Empty {
            empty: true,
            error: "Keine Daten für den Zeitraum",
        }));
    }

    let metrics = overview_metrics(&pool, &since, login_ref)
        .await
        .map_err(|_| ApiError::internal())?
        .ok_or_else(ApiError::internal)?;

    Ok(Json(OverviewResponse::Data(OverviewData {
        streamer: params.streamer,
        days,
        summary: OverviewSummary {
            avg_viewers:         metrics.avg_avg_viewers.unwrap_or(0.0),
            peak_viewers:        metrics.max_peak_viewers.unwrap_or(0),
            total_hours_watched: metrics.total_hours_watched.unwrap_or(0.0),
            total_airtime:       metrics.total_airtime_hours.unwrap_or(0.0),
            followers_delta:     metrics.total_followers.unwrap_or(0),
            total_sessions:      metrics.session_count.unwrap_or(0),
        },
    })))
}
```

### Test (hermetisch)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use axum::{
        body::Body,
        extract::ConnectInfo,
        http::{Request, StatusCode},
        routing::get,
        Extension, Router,
    };
    use sqlx::PgPool;
    use std::net::SocketAddr;
    use tb_http_core::ExpectedToken;
    use tower::ServiceExt;

    async fn setup_pool() -> Option<PgPool> {
        let url = std::env::var("TB_TEST_DATABASE_URL").ok()?;
        PgPool::connect(&url).await.ok()
    }

    async fn setup_schema(pool: &PgPool) {
        sqlx::query(
            r#"
            CREATE TABLE IF NOT EXISTS twitch_stream_sessions (
                id BIGSERIAL PRIMARY KEY,
                streamer_login TEXT NOT NULL,
                started_at TIMESTAMPTZ NOT NULL,
                ended_at TIMESTAMPTZ,
                avg_viewers DOUBLE PRECISION,
                peak_viewers BIGINT,
                duration_seconds BIGINT,
                follower_delta BIGINT,
                followers_start BIGINT,
                followers_end BIGINT
            );
            TRUNCATE twitch_stream_sessions;
            INSERT INTO twitch_stream_sessions
                (streamer_login, started_at, ended_at, avg_viewers, peak_viewers,
                 duration_seconds, follower_delta, followers_start, followers_end)
            VALUES
                ('streamer_x', NOW() - INTERVAL '1 day', NOW() - INTERVAL '23 hours',
                 100.0, 200, 3600, 5, 1000, 1005);
            "#,
        )
        .execute(pool)
        .await
        .unwrap();
    }

    fn router(pool: PgPool, token: &str) -> Router {
        Router::new()
            .route("/twitch/api/v2/overview", get(overview_handler))
            .with_state(pool)
            .layer(Extension(ExpectedToken(token.to_string())))
    }

    fn admin_req(token: &str, streamer: &str) -> Request<Body> {
        let addr: SocketAddr = "1.2.3.4:9999".parse().unwrap();
        Request::builder()
            .uri(format!("/twitch/api/v2/overview?streamer={}", streamer))
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, "example.com")
            .header("x-internal-token", token)
            .body(Body::empty())
            .unwrap()
    }

    #[tokio::test]
    async fn returns_401_without_auth() {
        let pool = match setup_pool().await {
            Some(p) => p,
            None => return,
        };
        let addr: SocketAddr = "1.2.3.4:9999".parse().unwrap();
        let req = Request::builder()
            .uri("/twitch/api/v2/overview?streamer=x")
            .extension(ConnectInfo(addr))
            .header(axum::http::header::HOST, "example.com")
            .body(Body::empty())
            .unwrap();
        let res = router(pool, "tok").oneshot(req).await.unwrap();
        assert_eq!(res.status(), StatusCode::UNAUTHORIZED);
    }

    #[tokio::test]
    async fn returns_empty_for_unknown_streamer() {
        let pool = match setup_pool().await {
            Some(p) => p,
            None => return,
        };
        setup_schema(&pool).await;
        let res = router(pool, "tok")
            .oneshot(admin_req("tok", "nobody"))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 512).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        assert_eq!(v["empty"], true);
    }

    #[tokio::test]
    async fn returns_metrics_for_known_streamer() {
        let pool = match setup_pool().await {
            Some(p) => p,
            None => return,
        };
        setup_schema(&pool).await;
        let res = router(pool, "tok")
            .oneshot(admin_req("tok", "streamer_x"))
            .await
            .unwrap();
        assert_eq!(res.status(), StatusCode::OK);
        let b = axum::body::to_bytes(res.into_body(), 1024).await.unwrap();
        let v: serde_json::Value = serde_json::from_slice(&b).unwrap();
        assert_eq!(v["summary"]["avgViewers"], 100.0);
        assert_eq!(v["summary"]["totalSessions"], 1);
    }
}
```

---

## Schritt 6: Router + Binary + Cargo-Updates

### `tb-dashboard-api/src/lib.rs` erweitern

`build_authed_router` ergänzen und die `build_router`-Merge-Funktion hinzufügen:

```rust
pub mod auth;
pub mod handlers;

use axum::{routing::get, Extension, Router};
use sqlx::PgPool;
use tb_http_core::ExpectedToken;
use tower_http::cors::CorsLayer;

pub fn build_public_router(pool: PgPool) -> Router {
    // ... bestehendes unverändert ...
}

/// Baut den Router für Admin-geschützte Routes.
///
/// Auth-Level wird per Extension eingesetzt — kein `middleware::from_fn_with_state`-Layer
/// nötig, da `AuthLevel` als `FromRequestParts` den Token selbst aus der Extension liest.
pub fn build_authed_router(pool: PgPool, token: String) -> Router {
    use handlers::{auth_status, overview, streamers};

    Router::new()
        .route(
            "/twitch/api/v2/auth-status",
            get(auth_status::auth_status_handler),
        )
        .route(
            "/twitch/api/v2/streamers",
            get(streamers::streamers_handler),
        )
        .route(
            "/twitch/api/v2/overview",
            get(overview::overview_handler),
        )
        .with_state(pool)
        .layer(Extension(ExpectedToken(token)))
}

/// Zusammengeführter Router: public + authed, kein doppelter CORS-Layer.
pub fn build_router(pool: PgPool, token: String) -> Router {
    build_public_router(pool.clone())
        .merge(build_authed_router(pool, token))
        .layer(CorsLayer::permissive())
}
```

### `bin/tb-dashboard/src/main.rs` anpassen

`build_router` statt `build_public_router` verwenden:

```rust
use tb_config::AppConfig;
use tb_dashboard_api::build_router;

let config = AppConfig::from_env().expect("Konfiguration laden");
let pool = /* bestehende Pool-Erstellung */;
let token = config.internal_api.token.clone();

let app = build_router(pool, token);
// ... bestehender axum::serve-Aufruf unverändert ...
```

### `tb-dashboard-api/Cargo.toml` ergänzen

```toml
[dependencies]
# ... bestehende deps ...
tb-http-core = { workspace = true }
tb-config    = { workspace = true }
chrono       = { workspace = true }

[dev-dependencies]
tokio     = { workspace = true }
tb-db     = { workspace = true }
tb-config = { workspace = true }
```

### `rust/Cargo.toml` — `chrono` als Workspace-Dep

Falls noch nicht vorhanden, unter `[workspace.dependencies]` ergänzen:

```toml
chrono = { version = "0.4", features = ["serde"] }
```

---

## Schritt 7: `handlers/mod.rs` aktualisieren

```rust
pub mod auth_status;
pub mod bans;
pub mod network;
pub mod overview;
pub mod raids;
pub mod streamers;
```

---

## Schritt 8: QS-Lauf

```bash
cd /home/naniadm/Documents/Deadlock-Twitch-Bot/rust && source "$HOME/.cargo/env"

# Format-Check
cargo fmt --all -- --check

# Clippy (alle betroffenen Crates)
cargo clippy \
  -p tb-http-core \
  -p tb-analytics \
  -p tb-dashboard-api \
  -p tb-dashboard \
  --all-targets -- -D warnings

# Unit-Tests (ohne DB)
cargo test -p tb-http-core -p tb-dashboard-api -- --nocapture

# Hermetische Integrations-Tests (mit Container)
bash scripts/test_db.sh up
TB_TEST_DATABASE_URL="postgres://postgres:tbtest@127.0.0.1:5434/postgres" \
  cargo test -p tb-analytics -p tb-dashboard-api -- --nocapture
bash scripts/test_db.sh down
```

Expected: fmt sauber, clippy 0 Warnungen, alle Tests grün.

---

## Docs-Sync

Nach der Implementierung folgende Architektur-Doku-Dateien aktualisieren:

- `rust/docs/architecture/tb-dashboard-api.md` — neue Routen + `AuthLevel`-Erklärung + Plan-Gating ergänzen
- `rust/docs/architecture/tb-http-core.md` — `AuthLevel` Extractor + `ExpectedToken` dokumentieren
- `rust/docs/architecture/tb-analytics.md` — `streamers` + `overview` Module ergänzen

---

## Dateistruktur nach 1b

```
rust/
  Cargo.toml                              ← chrono als workspace dep (falls fehlt)
  crates/
    tb-http-core/
      src/
        middleware/
          auth.rs                         ← AuthLevel + ExpectedToken ergänzt
          mod.rs                          ← pub use AuthLevel, ExpectedToken
        lib.rs                            ← pub use AuthLevel, ExpectedToken
    tb-analytics/
      src/
        lib.rs                            ← pub mod streamers; pub mod overview;
        streamers.rs                      ← StreamerListRow + active_streamers()
        overview.rs                       ← OverviewMetricsRow + overview_metrics() + overview_session_count()
    tb-dashboard-api/
      Cargo.toml                          ← tb-http-core, tb-config, chrono ergänzt
      src/
        lib.rs                            ← build_authed_router + build_router ergänzt
        auth.rs                           ← require_owner + require_extended_plan
        handlers/
          mod.rs                          ← auth_status, overview, streamers ergänzt
          auth_status.rs                  ← auth_status_handler
          streamers.rs                    ← streamers_handler
          overview.rs                     ← overview_handler
  bin/
    tb-dashboard/
      src/
        main.rs                           ← build_router statt build_public_router
  docs/
    plans/
      2026-06-09-schritt-1b.md            ← dieser Plan
    architecture/
      tb-dashboard-api.md                 ← aktualisiert
      tb-http-core.md                     ← aktualisiert
      tb-analytics.md                     ← aktualisiert
```
